# === COINFLIP PRODUCTION CONFIGURATION ===

# === NETWORK CONFIGURATION ===
# For testing: devnet | For production: mainnet-beta
SOLANA_NETWORK=mainnet-beta
# SOLANA_NETWORK=devnet  # For testing

# === RPC ENDPOINTS ===
# Primary RPC (Helius - get from https://www.helius.dev/)
RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_API_KEY
HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_API_KEY

# Backup RPC (QuickNode - get from https://www.quicknode.com/)
BACKUP_RPC_URL_1=https://your-endpoint.solana-mainnet.quiknode.pro/YOUR_QUICKNODE_KEY/
QUICKNODE_RPC_URL=https://your-endpoint.solana-mainnet.quiknode.pro/YOUR_QUICKNODE_KEY/

# Fallback RPC (Public - rate limited but free)
BACKUP_RPC_URL_2=https://api.mainnet-beta.solana.com

# === ENCRYPTION ===
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# CRITICAL: Back up this key in 3+ secure locations! If lost, cannot decrypt wallets!
ENCRYPTION_KEY=YOUR_FERNET_KEY_HERE

# === WALLET CONFIGURATION ===
# Treasury Wallet (Ledger - receives all platform fees)
# IMPORTANT: Use Ledger hardware wallet for production security!
TREASURY_WALLET=N7G9UdmkpsFkyzJpT74NpJ3Dghjt1iBeL4mTtwCunL3

# DO NOT set TREASURY_WALLET_SECRET in production!
# Use Ledger for signing (see LEDGER_WALLET_INTEGRATION.md)
# TREASURY_WALLET_SECRET=  # Leave empty - use Ledger!

# === ADMIN CONFIGURATION ===
ADMIN_EMAIL=your_admin_email@example.com

# === SMTP CONFIGURATION (for 2FA) ===
# Gmail (for testing - use SendGrid/SES for production)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_gmail_app_password  # Get from: https://myaccount.google.com/apppasswords

# Production SMTP (SendGrid recommended)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=YOUR_SENDGRID_API_KEY

# === PLATFORM CONFIGURATION ===
PLATFORM_NAME=Coinflip
PLATFORM_URL=https://yourdomain.com  # Update with your domain

# === TELEGRAM BOT ===
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN  # Get from @BotFather
TELEGRAM_BOT_ENABLED=true

# Bot mode: polling (for testing) | webhook (for production)
TELEGRAM_BOT_MODE=webhook
TELEGRAM_WEBHOOK_URL=https://api.yourdomain.com/telegram/webhook

# === RATE LIMITING ===
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60

# === LOGGING ===
LOG_LEVEL=INFO
# LOG_LEVEL=DEBUG  # For detailed debugging

# === DATABASE ===
DB_PATH=coinflip.db

# === API SERVER ===
API_HOST=0.0.0.0
API_PORT=8000

# === BACKUP CONFIGURATION ===
BACKUP_ENABLED=true
BACKUP_INTERVAL_HOURS=6
BACKUP_RETENTION_DAYS=30
BACKUP_LOCATION=backups/

# === AWS KMS (Optional but recommended) ===
# See AWS_KMS_SETUP.md for configuration
# AWS_REGION=us-east-1
# AWS_KMS_KEY_ID=your-kms-key-id
# USE_KMS_ENCRYPTION=true
